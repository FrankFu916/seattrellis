{"default":{"identifier":"default","description":"Default capability for the main SeatTrellis window. The workbench page is served by the loopback backend (WebviewUrl::External), which Tauri classifies as a remote origin, so the file-bridge commands must be granted for that origin explicitly. Byte-moving commands additionally verify dialog-granted paths in Rust.","remote":{"urls":["http://127.0.0.1:*","http://localhost:*"]},"local":true,"windows":["main"],"permissions":["core:default","allow-workbench-files"]}}