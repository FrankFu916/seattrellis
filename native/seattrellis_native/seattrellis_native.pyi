def assignment_is_unique(
    student_count: int,
    seat_count: int,
    assignments: list[tuple[int, int]],
) -> bool: ...

def seat_distance(
    first_row: float,
    first_col: float,
    second_row: float,
    second_col: float,
) -> float: ...
