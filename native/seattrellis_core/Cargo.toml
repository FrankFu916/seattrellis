[package]
name = "seattrellis_core"
version = "0.1.0"
edition = "2021"
rust-version = "1.83"
license = "Apache-2.0"
description = "Experimental pure Rust core helpers for SeatTrellis."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
