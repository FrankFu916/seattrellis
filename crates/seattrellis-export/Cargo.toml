[package]
name = "seattrellis-export"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Export/render primitives: solved plan to SVG / HTML / PNG / PDF bytes (M1-02 crate split)."

[dependencies]
seattrellis_core = { path = "../../native/seattrellis_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
png = "0.17"
