[package]
name = "seattrellis-domain"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Domain layer: editor state machine, room templates and goal rules (M1-02 crate split)."

[dependencies]
seattrellis_core = { path = "../../native/seattrellis_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
