[package]
name = "seattrellis-application"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Application layer: use-case orchestration for generation and export (M1-02 crate split)."

[dependencies]
seattrellis_core = { path = "../../native/seattrellis_core" }
seattrellis-domain = { path = "../seattrellis-domain" }
seattrellis-export = { path = "../seattrellis-export" }
serde_json = "1"
