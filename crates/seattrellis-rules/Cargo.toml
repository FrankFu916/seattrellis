[package]
name = "seattrellis-rules"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "RuleSpec metadata registry: the single source of truth for every official rule (M3-01)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = { version = "1", features = ["derive"] }

[dev-dependencies]
jsonschema = "0.30"
