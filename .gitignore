**/__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.e2e-tmp/
.e2e-rust-tmp/
.mypy_cache/
.ruff_cache/
.venv/
venv/
dist/
site/
build/
target/
test-results/
test-results-rust/
outputs/
exports/
snapshots/
*.snapshot.json
!examples/history/*.snapshot.json
# The parity history corpus is synthetic release evidence, not a user output.
# It must be committed so a clean checkout can replay history-dependent cases.
!fixtures/parity/inputs/*/history/*.snapshot.json
# The artifact-parity corpus carries its own outputs/ directory; it is
# synthetic oracle evidence, not a user output.
!fixtures/artifact-parity/outputs/
data/
private/
real_students/
real_classes/
.env
.DS_Store
node_modules/
.workbuddy/
.idea/
.vscode/
.reasonix/
.zcode/
.claude/

# Frozen oracle venv (M6: installed from the v1.9.0 tag for differentials)
.oracle-venv/

# Local agent/session state

# Private local mirror of the internal planning/decision docs (restored from
# git history; intentionally never tracked or pushed to GitHub).
docs-internal/

# Docusaurus docs site (website/) build cache; the build/ output dir is
# already covered by the global `build/` entry above.
website/.docusaurus/
website/node_modules/
